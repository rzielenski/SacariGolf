# Copy this file to `.env.local` (gitignored) and fill in real values.
# app.config.js reads these at build time.

# Android Google Maps SDK key. Restrict it in Google Cloud to package
# com.sacari.app + your signing SHA-1(s). iOS does NOT use this (Apple Maps).
GOOGLE_MAPS_ANDROID_KEY=your-android-google-maps-key
