{
  "name": "sacari-web",
  "version": "1.0.0",
  "private": true,
  "description": "Public web profiles for Sacari Golf (sacarigolf.com/u/<username>). Standalone from the mobile app and API. Server-rendered for rich link previews and SEO. Reads the same Postgres database read-only.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
